// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update `site` to your final Vercel/custom URL for correct canonical + OG tags.
  site: 'https://phing-shuin.vercel.app',
});
